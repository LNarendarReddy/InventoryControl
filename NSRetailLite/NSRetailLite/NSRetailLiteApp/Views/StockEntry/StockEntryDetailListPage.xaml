<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="NSRetailLiteApp.Views.StockEntry.StockEntryDetailListPage"
             xmlns:views="clr-namespace:NSRetailLiteApp.Views.StockEntry"
             Title="StockEntryDetailListPage" Loaded="ContentPage_Loaded">
    <ContentPage.Resources>
        <ResourceDictionary>
            <toolkit:InvertedBoolConverter x:Key="InvertedBoolConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>
    <Grid>
        <Grid RowDefinitions="Auto, *, Auto, Auto">
            <VerticalStackLayout Grid.Row="0" Spacing="5">
                <SearchBar x:Name="searchBar" 
               SearchCommand="{Binding Path=PerformSearchCommand}"                   
               SearchCommandParameter="{Binding Text, Source={x:Reference searchBar}}"
               Placeholder="{Binding Path=StockEntryModel.SupplierName}" 
               PlaceholderColor="White" CancelButtonColor="White">
                    <SearchBar.Behaviors>
                        <toolkit:EventToCommandBehavior
                                Command = "{Binding PerformSearchCommand}"
                                CommandParameter="{Binding Text, Source={x:Reference searchBar}}"
                                EventName="TextChanged" />  
                    </SearchBar.Behaviors>
                </SearchBar>
                <Border Margin="10, 0, 10, 0" Padding="5" StrokeThickness="3" Stroke="PaleVioletRed">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="10" />
                    </Border.StrokeShape>
                    
                    <Grid  ColumnDefinitions="auto, auto, auto, *, auto" HorizontalOptions="FillAndExpand">
                        <Label Text="Invoice #: " VerticalTextAlignment="Center" FontSize="Subtitle" Grid.Column="0"/>
                        <Label Text="{Binding Path=StockEntryModel.SupplierInvoiceNo}" FontAttributes="Bold"  
                           VerticalTextAlignment="Center" FontSize="Subtitle" Grid.Column="1"/>
                        <Label Text="{Binding Path=StockEntryModel.InvoiceDate, StringFormat=' ( {0:d} )'}" 
                           VerticalTextAlignment="Center" FontSize="Subtitle" Grid.Column="2"/>
                        <Button Text="Edit Invoice #" HorizontalOptions="End" Grid.Column="3" Command="{Binding Path=EditInvoiceNoCommand}"/>
                    </Grid>
                </Border>
                <Rectangle HeightRequest="2" BackgroundColor="PaleVioletRed" Margin="10, 0, 10, 0" />
            </VerticalStackLayout>
            <CollectionView ItemsSource="{Binding Path=FilteredStockEntryDetails}" Grid.Row="1" x:Name="mCollectionView">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <SwipeView>
                            <SwipeView.RightItems>
                                <SwipeItem 
                                    IconImageSource="stockcountingdetailedit.png"
                                    Command="{Binding Path=BindingContext.EditStockEntryDetailCommand, Source={RelativeSource AncestorType={x:Type views:StockEntryDetailListPage}}}"
                                    CommandParameter="{Binding .}" BackgroundColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}"/>
                                <SwipeItem 
                                    IconImageSource="stockcountingdetaildiscard.png"                                 
                                    Command="{Binding Path=BindingContext.DeleteStockEntryDetailCommand, Source={RelativeSource AncestorType={x:Type views:StockEntryDetailListPage}}}"
                                    CommandParameter="{Binding .}" BackgroundColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}"/>
                            </SwipeView.RightItems>

                            <Grid Padding="10, 5, 10, 5" RowDefinitions="Auto, Auto, Auto" ColumnDefinitions="Auto, *, Auto, Auto, Auto">
                                <Label Text="{Binding Path=SNo, StringFormat='{0}.'}" Margin="0, 0, 5, 0" 
                                Grid.Row="0" Grid.RowSpan="2" Grid.Column="0" VerticalOptions="Center" VerticalTextAlignment="Center"/>

                                <Label Text="{Binding Path=ItemCode}" Margin="0, 0, 5, 0" Grid.Row="0" Grid.Column="1" HorizontalOptions="StartAndExpand"/>
                                <Label Text="{Binding Path=MRP, StringFormat='MRP: {0}'}" Margin="0, 0, 10, 0" Grid.Row="0" Grid.Column="2" FontSize="Caption" HorizontalOptions="End"/>
                                <Label Text="{Binding Path=Quantity, StringFormat='Qty: {0}'}" Margin="0, 0, 10, 0" Grid.Row="0" Grid.Column="3" 
                                       HorizontalOptions="End" FontSize="Caption" FontAttributes="Bold" 
                                       IsVisible="{Binding Path=IsOpenItem, Converter={StaticResource Key=InvertedBoolConverter}}"/>
                                <Label Text="{Binding Path=WeightInKGs, StringFormat='Wght: {0}'}" Margin="0, 0, 10, 0" Grid.Row="0" Grid.Column="3" 
                                       HorizontalOptions="End" FontSize="Caption" FontAttributes="Bold"
                                       IsVisible="{Binding Path=IsOpenItem}"/>

                                <Label Text="{Binding Path=ItemName}" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="4" LineBreakMode="TailTruncation" />
                                <Rectangle HeightRequest="2" BackgroundColor="PaleVioletRed" Grid.Row="2" Grid.ColumnSpan="5"/>
                            </Grid>
                        </SwipeView>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <Grid Grid.Row="2" BackgroundColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}"
          RowDefinitions="60" ColumnDefinitions="20, *, *, *, 20">
                <VerticalStackLayout Grid.Row="0" Grid.Column="1" HorizontalOptions="Center" VerticalOptions="Center">
                    <ImageButton Source="stockcountingadd.png" Aspect="Fill" HeightRequest="30" WidthRequest="30" Command="{Binding Path=AddStockEntryDetailCommand}"/>
                    <Label Text="Add Item" TextColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource White}}">
                        <Label.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding AddStockEntryDetailCommand}" />
                        </Label.GestureRecognizers>
                    </Label>
                </VerticalStackLayout>
                <VerticalStackLayout Grid.Row="0" Grid.Column="2" HorizontalOptions="Center" VerticalOptions="Center">
                    <ImageButton Source="stockcountingsubmit.png" Aspect="Fill" HeightRequest="30" WidthRequest="30" Command="{Binding Path=SubmitStockEntryCommand}"/>
                    <Label Text="Submit" TextColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource White}}">
                        <Label.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding SubmitStockEntryCommand}" />
                        </Label.GestureRecognizers>
                    </Label>
                </VerticalStackLayout>
                <VerticalStackLayout Grid.Row="0" Grid.Column="3" HorizontalOptions="Center" VerticalOptions="Center">
                    <ImageButton Source="stockcountingdiscard.png" Aspect="Fill" HeightRequest="30" WidthRequest="30" Command="{Binding Path=DiscardStockEntryCommand}"/>
                    <Label Text="Discard" TextColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource White}}">
                        <Label.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding DiscardStockEntryCommand}" />
                        </Label.GestureRecognizers>
                    </Label>
                </VerticalStackLayout>
            </Grid>
        </Grid>
        <ActivityIndicator IsRunning="{Binding Path=IsLoading}" WidthRequest="150"/>
    </Grid>
</ContentPage>